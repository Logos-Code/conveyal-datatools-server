commands:
  create:
    command: "mkdir /tmp/gtfs-data"
    ignoreErrors: true
  setup_chown:
    command: "chown -R webapp:webapp /tmp/gtfs-data"
    ignoreErrors: true
  setup_chmod:
    command: "chmod 777 /tmp/gtfs-data"
    ignoreErrors: true